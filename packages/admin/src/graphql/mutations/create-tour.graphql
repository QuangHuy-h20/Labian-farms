mutation CreateTour(
  $createTourInput: CreateTourInput!
  $file: Upload!
  $farmId: ID!
) {
  createTour(createTourInput: $createTourInput, file: $file, farmId: $farmId) {
    ...tourMutationResponse
  }
}
