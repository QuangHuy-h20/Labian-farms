mutation CreateProduct(
  $farmId: ID!
  $createProductInput: CreateProductInput!
  $file: Upload!
) {
  createProduct(file: $file, createProductInput: $createProductInput, farmId: $farmId) {
  ...productMutationResponse
  }
}
