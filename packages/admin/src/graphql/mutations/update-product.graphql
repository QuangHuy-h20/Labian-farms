mutation UpdateProduct(
  $updateProductInput: UpdateProductInput!
  $files: [Upload!]!
) {
  updateProduct(
    updateProductInput: $updateProductInput
    files: $files
  ) {
    ...productMutationResponse
  }
}
